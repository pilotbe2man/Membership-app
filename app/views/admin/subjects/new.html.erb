<% content_for :title, 'New Subject' %>
<% content_for :breadcrumb, 'New' %>
<% breadcrumb_add 'Subjects', admin_subjects_path %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header"><%= t('admin.subjects.new.heading')%></h2>
    </div>
    <%= render 'form' %>
</div>

<% content_for :title, 'Edit Payment Mode' %>
<% content_for :breadcrumb, 'Edit' %>
<% breadcrumb_add 'Payment Mode', admin_payment_modes_path %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header"><%= t('admin.payment_modes.edit.heading')%></h2>
    </div>
    <%= render 'form' %>
</div>

<% content_for :title, 'Edit Discount Code' %>
<% content_for :breadcrumb, 'Edit' %>
<% breadcrumb_add 'Discount Codes', admin_discount_codes_path %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header"><%= t('admin.discount_codes.edit.heading')%></h2>
    </div>
    <%= render 'form' %>
</div>

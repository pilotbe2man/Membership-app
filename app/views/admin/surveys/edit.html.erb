<% content_for :title, 'Edit Survey Module' %>
<% content_for :breadcrumb, 'Edit' %>
<% breadcrumb_add 'Surveys', admin_survey_subjects_path %>
<% breadcrumb_add @subject.title, admin_survey_subject_path(@subject) %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header">Edit Survey Module</h2>
    </div>
    <%= render partial: 'form', locals: { url: admin_survey_subject_survey_path } %>
</div>


.navbar-header
  %button.navbar-toggle.collapsed{'data-toggle': 'collapse', 'data-target': '#navbar', 'aria-expanded': false, 'aria-controls': 'navbar'}
    %span.sr-only Toggle Navigation
    %span.icon-bar
    %span.icon-bar
    %span.icon-bar
    %span.icon-bar

#navbar.navbar-collapse.collapse
  %ul.nav.navbar-nav
    %li
      = link_to '#' do
        %i.glyphicon.glyphicon-plus
        Add Child

    - current_user.children.each do |child|
      %li{class: child.id == @active_child.id ? 'active' : ''}
        = link_to parentee_discussions_path(child_id: child.id), 'data-child_id': child.id do
          = child.name

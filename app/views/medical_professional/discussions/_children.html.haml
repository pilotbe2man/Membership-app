.panel.panel-default
  .panel-heading
    Children
  .panel-body
    %ul.list-group.children
      - @children.each do |child|
        %li.list-group-item.child{class: child.id == @active_child.id ? 'active' : ''}
          = link_to medical_professional_discussions_path(child_id: child.id) do
            = image_tag child.profile_image.file.url, class: 'img'
            = child.name
